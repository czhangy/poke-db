import ruby_sapphire from "@/data/segments/ruby_sapphire";
import Split from "@/models/Split";

const splits: { [game: string]: Split[] } = {
    ruby_sapphire: ruby_sapphire,
};

export default splits;
